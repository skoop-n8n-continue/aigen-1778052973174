document.addEventListener('DOMContentLoaded', () => {
    const messageElement = document.getElementById('message');
    if (messageElement) {
        // The message is already set in HTML, but JS can be used for more dynamic content or animations.
        // For this 'Hello World' case, we'll just ensure it's visible.
        console.log('Digital Signage App loaded.');
    }
});
